---
export const prerender = false;
// Astro excludes underscore-prefixed route segments from its page graph. This
// explicit, documented equivalent is used by the guarded editor E2E fixture.
const runtimeProcess = (
  globalThis as typeof globalThis & {
    process?: { env?: Record<string, string | undefined> };
  }
).process;
if (runtimeProcess?.env?.ALLOW_FIXTURE_CONTENT !== 'true') {
  return new Response(null, { status: 404 });
}
import EditorFixtureApp from '@/components/admin/EditorFixtureApp';
---

<!doctype html>
<html lang="zh-Hant-TW">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Editor fixture</title>
  </head>
  <body><EditorFixtureApp client:load /></body>
</html>
